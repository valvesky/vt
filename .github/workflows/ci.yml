name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: deps
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc fonts-dejavu-core libx11-dev
      - name: font
        run: |
          mkdir -p fonts
          ln -sf /usr/share/fonts/truetype/dejavu/DejaVuSansMono.ttf fonts/iosevka-mono.ttf
      - name: build
        run: gcc -o build build.c && ./build headless
      - name: headless
        run: sh tests/headless

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: font
        run: |
          mkdir -p fonts
          ln -sf /System/Library/Fonts/Supplemental/Courier\ New.ttf fonts/iosevka-mono.ttf
      - name: build
        run: cc -o build build.c && ./build headless
      - name: headless
        run: sh tests/headless

  windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc
      - name: font
        run: |
          mkdir -p fonts
          cp /c/Windows/Fonts/consola.ttf fonts/iosevka-mono.ttf
      - name: build
        run: gcc -o build build.c && ./build headless
      - name: headless
        run: sh tests/headless
