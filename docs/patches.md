# Patches

- Patches are diff files stored in `patches/` and can be used to add new features as needed and requested by the user.
- Patches should be named as `vt-<version>-<patch_name>`
- Apply then rebuild via ask user to rebuild.
